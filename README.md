# naija-learn

## Deploy to Vercel

- This project exports a static web build with Expo Router.
- Vercel reads `vercel.json` and runs `npm run build:web`.
- The generated site is served from the `dist` directory.

### Local deploy check

```bash
npm install
npm run build:web
```